# Build Plan

Gates are non-negotiable; discretion zones are the agent and user's call, recorded in `/docs/adr/`.

Complete `10_DAY0_GATES.md` first.

## Calendar (corrected Sept 7, verified against ethglobal.com/events/ethonline2026)

The event page lists the *finale* on Sept 16, but **project submissions close Sept 13, 16:00 UTC** — the original "Days 10–12" Phase 6 window (Sept 13–15) was scheduled entirely after the deadline and no longer exists. Real build time from Sept 7: **6.5 days.**

| Date (UTC) | Date (IST) | Milestone |
|---|---|---|
| Sept 4–6 | Sept 4–6 | Phase 1 complete (Days 1–3); Gates 0.2/0.3, 1.1–1.4 done |
| Sept 7 | Sept 7 | Watch the Ledger "Tracks Explained" recording (aired Sept 7 14:00 UTC / 19:30 IST — may resolve the `wallet-cli send` calldata question); resolve Gate 0.1; **run Gate 0.4 (Blocky402)** |
| **Sept 8, 03:59** | **Sept 8, 09:29** | **ETHGlobal Project Check-in #1 due** (showcase checkpoint — not optional) |
| Sept 8, 18:00 | Sept 8, 23:30 | Project Feedback Session #1 (cheap judge-proxy scrutiny — use it on the Phase 2 ACL story) |
| Sept 10, 13:00 | Sept 10, 18:30 | Project Feedback Session #2 |
| **Sept 11, 03:59** | **Sept 11, 09:29** | **ETHGlobal Project Check-in #2 due** |
| Sept 12 | Sept 12 | Build freeze. Record the video (human voice, 2–4 min; Hedera ≤5) |
| **Sept 13, 16:00** | **Sept 13, 21:30** | **Project submissions due** (video attached — `requireVideoSubmission` is on). Judging Round 1 (async) starts 19:00 UTC / Sept 14, 00:30 IST |
| Sept 14, 16:00 | Sept 14, 21:30 | Judging Round 2: live project judging |
| Sept 16, 16:00 | Sept 16, 21:30 | ETHOnline 2026 Finale (nothing is due after Sept 13) |

## Phase 1 — Node agent core (Days 1–3 — Sept 4–6; ✅ complete)

Userspace WireGuard, STUN, hole punching, relay fallback, and the admission verifier.

`HARD GATE 1.1 — The peer refuses independently.` A node must reject a handshake from a key that is not in the ENS registry, **using only its own resolution of that registry**. No central service participates in the decision.
**Test:** `test_PeerRejectsUnregisteredKey`. Stand up a node, present a valid, well-formed WireGuard handshake from an unregistered key, assert refusal. Then register the key, assert acceptance after cache expiry. This test is the project; film it.

`HARD GATE 1.2 — A lying coordinator changes nothing.` Run a deliberately hostile signaling relay that advertises an unauthorized peer as legitimate.
**Test:** `test_MaliciousRelayCannotAdmitPeer`. Assert the connection is refused. If this fails, the thesis is dead, not the test.
**✅ Verified Sept 6, 2026** — `brambled/rendezvous/gate1_2_test.go`'s `TestGate1_2_MaliciousRelayCannotAdmitPeer`. A hostile relay fabricates a response claiming Bob is at an attacker's address, without Bob ever being registered or involved at all. The attacker is a real, live, responsive `wgnode.Node` — not a stub. Alice still addresses her handshake to Bob's real public key (from ENS, independent of the relay); the attacker doesn't hold Bob's private key, so Noise_IK never completes and no handshake is ever recorded. This backs `docs/adr/0003-rendezvous-relay-split.md`'s "Trust boundary" claim with code, not just prose.
**Caution, scope:** this gate covers admission *integrity* — a hostile relay cannot get an unauthorized peer accepted, because a substituted or wrong address just produces a WireGuard handshake that fails (the attacker doesn't hold the real peer's private key). It does not cover *availability*: a rogue or down rendezvous relay can still deny connectivity for pairs routed through it (see `adr/0003-rendezvous-relay-split.md`, "Trust boundary"). Mitigate structurally: a tailnet's published rendezvous set must have more than one relay, and node agents must attempt candidate exchange across the whole set (parallel or sequential retry) rather than only the first one reached — one rogue or unreachable relay must not be able to unilaterally block a connection another relay in the set would have carried.
**✅ Failover mitigation built Sept 10, 2026** — `brambled/main.go`'s `exchangeAnyMetered` (originally `rendezvous.ExchangeAny`, renamed the same day when per-relay token pairing replaced it — see Section D below) tries every rendezvous relay `brambled` knows about (discovered from the relay registry, or `-rendezvous` overrides) concurrently, each getting the full timeout, so one unreachable relay can't make the whole attempt take len(set)×timeout. See `docs/adr/0003`'s Consequence section.

`HARD GATE 1.3 — Revocation propagates and is bounded.` Removing a device from the registry causes peers to drop it within a stated, measured window.
**Test:** measure the actual time from revocation transaction confirmation to connection drop. **Publish the number in the README.** It will be slower than Tailscale's push-based revocation. Say so; do not hide it (see `09_JUDGING.md`, objection 4).
**Caution:** ENSv2 token IDs are mutable — an EAC role grant or revoke burns and re-mints the token with a new ID (see `11_SOURCE_NOTES.md`). Detect revocation by re-reading the name's current role set, not by watching one token ID for a Transfer/burn event — an unrelated role change on the same name would emit an identical-looking event. **Not an issue here** — `sidecar/src/ens/client.ts`'s `resolveDevice` reads `getState(labelhash)` fresh by label every call, never by a cached token ID.
**Caution, design gap:** WireGuard has no teardown/revocation message. An already-established tunnel keeps passing traffic until either the local peer entry is actively removed from the wireguard-go config, or WireGuard's own rekey timer fires (~2 min by default). If the admission verifier only gates *new* handshakes, "time to connection drop" reads fast for a peer that hasn't connected yet, while an *already-connected* revoked peer stays up for up to ~2 minutes regardless — a different, more attack-relevant number. The registry-refresh loop must actively remove revoked peers from the live wireguard-go peer table on every poll, not just refuse future handshakes. State explicitly which scenario the measured number covers, and ideally measure both.
**✅ Mechanism verified Sept 6, 2026** — `brambled/admission/gate1_3_test.go`'s `TestGate1_3_RevocationDropsAlreadyConnectedPeer` proves the already-connected case drops immediately once `SyncOnce` removes the peer (bounded by `-ttl`, not the ~2 minute rekey timer), using a real `wgnode.Node` peer table, not a fake one. **Writing this test caught a real bug**: revoking by *clearing* a device's `pubkey` text record (the only proven ENS write primitive available — no verified `setExpiry`/`renew`-to-the-past exists) left the peer stuck in the live table forever, because the loop had no way to recover which pubkey to remove once the record stopped reporting one. Fixed in `admission/loop.go` by having `Loop` remember the last pubkey each label was authorized under. **✅ Live number measured Sept 6, 2026** — real VPS (device2) + laptop (device1) run, `-ttl 5s`, following `brambled/README.md`'s "Gate 1.3 runbook." Revoked device2 by clearing its `pubkey` text record via `set-pubkey.ts`; on-chain confirmation at `11:37:01.819Z`. Timestamped `ping -D` on the laptop showed the last successful reply at `11:37:03.54Z` (+1.7s) and permanent connection drop (zero recovery afterward) at `11:37:05.53Z` (**+3.7s from revocation confirmation to connection drop, already-connected peer**) — bounded by the 5s `-ttl` used, nowhere near WireGuard's ~2 minute rekey timer. Slower than Tailscale's push-based revocation, as expected; this is the cost of "no trusted coordinator to push a revoke," and it's still sub-4-second at a demo-realistic `-ttl`.

`HARD GATE 1.4 — No endpoints on device or agent subnames.` Grep the codebase: no IP address, port, or endpoint is ever written to a device or agent (mesh-member) ENS record. Relay subnames are the sole exception (see `03_ARCHITECTURE.md`) — any write path touching a relay's record must be structurally incapable of also touching a device/agent record (separate sub-registry, not just a runtime check).
**Test:** CI check that fails on any write path writing endpoint data into the device/agent sub-registry namespace. Also a manual read of every record the demo writes, confirming relay and device/agent records never share a namespace.
**✅ Verified Sept 6, 2026** — `scripts/check-no-endpoints.mjs`, wired into `pnpm verify`/CI. Scans every `setText()` call site under `sidecar/src`, `scripts/provision-dev-tailnet`, and `brambled` (excluding the frozen `scripts/gate0.3-eac-check` artifact and `relay/`, the one namespace allowed to carry endpoint data) for a banned record key (`endpoint`/`address`/`ip`/`host`/`port`/`url`) or an IP:port-shaped literal argument. Manual read confirms the only current writes (`register-device2.ts`, `index.ts`) ever write `pubkey`, never endpoint data. Known limit, stated plainly: a value passed through a variable rather than a literal can't be caught by grep — this check is a regression gate against a *known-shape* violation, not a substitute for the manual read.
**✅ Relay ENS write path added and manually read, Sept 10, 2026** — exactly the case this gate's own write-up anticipated. `admincli/src/setup-relay-registry.ts`/`register-relay.ts` write `rendezvous-address`/`sidecar-url`/`price-per-byte` — real endpoint data — but only ever to the relay registry's own resolvers (a `UserRegistry` deployed structurally separate from the tailnet's device registry, per this gate's own requirement — see `docs/adr/0003`'s Consequence section), never a device/agent one. Manually confirmed: `setup-relay-registry.ts`'s only `tailnetRegistry()` call registers the `relays` namespace pointer with `resolver=address(0)` (no `setText` on it at all); `register-relay.ts`'s three `setText` calls all target a freshly-deployed resolver on the relay registry, never `tailnetRegistry()`. Both files added to `check-no-endpoints.mjs`'s `EXCLUDE_PREFIXES`, explicitly, not relying on the grep's incidental blind spots.

**Discretion:** cache TTL, resolution strategy, ICE configuration, relay protocol framing. ENS interaction is decided, not discretionary: the node agent calls a local per-node Hono/TypeScript sidecar over HTTP rather than making contract calls directly — see `adr/0002-node-agent-language.md`.

## Phase 2 — ENSv2 registry and ACLs (Days 4–5 — Sept 7–8)

`HARD GATE 2.1 — EAC is load-bearing, not decorative.` Enrollment, revocation, and key rotation are governed by distinct EAC roles, and an account without a role genuinely cannot perform the action.
**Test:** table-driven test, one case per role × action, asserting permitted and denied cases on Sepolia. ENS judges explicitly require features to be central, not cosmetic.
**Caution:** every grant/revoke in this test changes the target token's ID (mutable token IDs, see `11_SOURCE_NOTES.md`). Re-query the current token ID after each step rather than asserting against one captured at test start.
**✅ Verified Sept 7, 2026** — `admincli/test/gate2.1-eac-check.ts`, run end-to-end against the live hackathon Sepolia deployment (real dev tailnet from `scripts/provision-dev-tailnet`, real transactions, real revert checks — not simulated setup). Three distinct EAC roles, one per action (design in `docs/adr/0004-admin-cli-writes-directly-to-registry.md`): `ROLE_REGISTRAR` (root-scoped on the subregistry, gates `register()`) for enroll, `ROLE_SET_TEXT` scoped to the `pubkey` setter for rotate, `ROLE_SET_TEXT` scoped to the `revoked` setter (a new record, additive to the existing clear-`pubkey` revocation path Gate 1.3 already verified) for revoke. Granted to three freshly generated, never-funded test accounts via three real on-chain transactions (`grantRootRoles`/`grantSetterRoles`), confirmed on Sepolia. All 12 cases in the {enroller, rotator, revoker, no-role} × {enroll, rotate, revoke} table matched expectation: each role permitted only its own action and was denied (`EACUnauthorizedAccountRoles`-class revert, via `simulateContract` — Gate 0.3's proven technique) every other action, and the no-role account was denied all three. `ROLE_REGISTRAR` was verified from `ensdomains/contracts-v2`'s primary source (`PermissionedRegistry.sol`, `RegistryRolesLib.sol`), not guessed or taken from the docs page alone — see ADR 0004 for why this mattered (it is a distinct grant target from the existing `REGISTRATION_ROLE_BITMAP`, which governs the new token owner's rights, not who may call `register()`).

`HARD GATE 2.2 — Nothing hard-coded in the demo path.` ENS qualification says the demo must be functional and not include hard-coded values.
**Test:** delete every local cache, restart both nodes, confirm the mesh reassembles purely from chain state.
**✅ Verified Sept 9, 2026** — structural check (`scripts/check-no-local-state.mjs`, wired into `pnpm verify`) proves the demo runtime path (`brambled/**/*.go` excluding tests, `sidecar/src/**/*.ts`) writes no local state at all; behavioral unit test (`brambled/admission/gate2_2_test.go`'s `TestGate2_2_FreshLoopReadsCurrentChainStateNotPriorMemory`) proves a zero-prior-state `Loop` admits only what the resolver reports now. Live two-machine confirmation against the real Sept 8 VPS (`13.207.155.93`): both nodes killed, `laptop-demo`'s on-chain pubkey cleared, both restarted fresh from identical flags — the VPS's brand-new `Loop` refused the peer outright (`admission: laptop-demo: not authorized`, real `curl` connection timeout, not just an ACL deny), then restoring the pubkey with **no restart** produced re-admission and a real `curl` 200 on the VPS's own next resync. Full transcript, real tx hashes, and the complete local-state inventory: `brambled/README.md`'s "Gate 2.2 runbook."

`HARD GATE 2.3 — ACLs are enforced at the peer.` A device may reach exactly what its ACL permits.
**Test:** `test_AgentCannotReachOutsideACL`. Agent with a `db` grant sends `CONNECT cache` on the same host and `CONNECT db` on a different host entirely; both refused by the receiving peer. (Record schema: symbolic service names published as ECDH digests between the vouching identity's and target host's already-published pubkeys, not `host:port` — see `adr/0005-acl-record-schema.md`, Phase 2 Section B. Each gateway verifies only against granters listed in its own `acl-granters` record — no tailnet-wide admin key involved.)
**✅ Verified Sept 8, 2026** — `brambled/gateway/server_test.go`'s `TestGate2_3_AgentCannotReachOutsideACL`, run against three real `wgnode.Node`s with real WireGuard handshakes (netstack, per existing Gate 1.2/1.3 precedent — not a live two-machine run, which isn't required for this gate). All three required cases pass: `CONNECT db` on the authorized gateway succeeds and proxies real bytes end to end from a real local backend process; `CONNECT cache` on the same gateway is denied; `CONNECT db` on a second, separately-keyed gateway that trusts the *same* granter is denied too — proving the ECDH digest is bound to one specific gateway pubkey, not just "an authorized granter said so." Full design and the gateway CONNECT protocol itself: `adr/0006-gateway-connect-protocol.md`. 11/11 tests pass in `brambled/gateway`; full `brambled` suite green, no regressions.

**Discretion:** record schema, whether to use wildcard resolution or a deployed subname registry, ENSIP-25/26 record keys (recommended), expiry granularity.

## Phase 3 — Ledger enrollment (Days 6–7 — Sept 9–10)

Skip entirely if Gate 0.1 failed.

**✅ Resumed and physically run Sept 10, 2026.** The Nano S Plus arrived; Gate 0.1's hands-on run happened for real (genuine-check, `ring init`, `ring encrypt`/`decrypt` including with the device physically unplugged, plus a real cross-machine test against the `Bramble` EC2 box). Full writeup: `.tmp/LEDGER_GATE0.1_REPORT.md`. **This run also forced a retarget of Gates 3.1–3.3** — see `adr/0001-ledger-ring-vs-send-split.md`'s "Pivot (Sept 10)" section for the full reasoning: cross-host `ring` decrypt does not work with `wallet-cli` as shipped (verified on real hardware, not assumed), so Gate 3.2's original target (the headless node's WireGuard key) is not achievable in the time remaining without reintroducing a centralized "decrypt-as-a-service" dependency that contradicts this project's own no-trusted-coordinator thesis — deliberately rejected, not merely deferred. Gate 3.2 is retargeted below at the admin's own granter key instead, which is same-host by design and a better Ledger-specific fit regardless (no signing device can custody an ECDH scalar — only Key Ring's encryption primitive can).

`HARD GATE 3.1 — Device confirmation gates permission changes.` Enrolling, revoking, or widening an agent's ACL scope requires a physical button press. No software path bypasses it.
**Test:** attempt any of the three with the device disconnected; assert failure.

**❌ BLOCKED Sept 10/11, 2026 — not achievable with shipped `wallet-cli` tooling, confirmed against primary source, not just an error message.** Built `admincli/src/ledger-send.ts` (wraps `wallet-cli send --data <calldata>`) and wired it into `enroll.ts`/`revoke.ts`/`set-acl.ts`, replacing their viem wallet clients. Every real attempt — a plain 0 ETH transfer, no calldata, dry-run and not — failed identically at fee estimation: `{"ok":false,"error":{"message":"FeeNotLoaded"}}`, before ever reaching the device (no on-device prompt). Traced the error class in Ledger's own `ledger-live` source (`libs/ledger-wallet-framework/src/errors.ts`, referenced from `coin-evm/src/prepareTransaction.ts`) to rule out a local misconfiguration, then found the actual cause in Ledger's own AI-agent skill, primary source: `LedgerHQ/agent-skills`, `skills/wallet-cli/wallet-cli-usage/SKILL.md` line 57, "Out of scope — say no, don't improvise": **`send`, `receive`, `operations`, or `swap execute` on testnets and layer 2s (e.g. Base)** are explicitly unsupported in `wallet-cli` 2.1.0. `account discover`/`balances` do work on Sepolia (confirmed live: `0x41DBf0A43173BF509759BfA7F89d66b02058A256`, 0.05 ETH, `ethereum:sepolia`) — only signing/broadcasting doesn't. **Reverted the code change** (the three scripts are back on viem/`SEPOLIA_PRIVATE_KEY` signing, matching every other admincli script) rather than ship a from-scratch EIP-1559 signer against `@ledgerhq/hw-app-eth` with two days left and the device only reachable through the user — that path also needs the Ethereum app open on-device, conflicting with `ring`'s Ledger Sync app requirement mid-flow, and would be untested, first-try, on camera. This is a documented tool limitation, not a design gap: ENS's own `ROLE_SET_TEXT` scoping already gives each admin action a narrowly-scoped key rather than an unrestricted one, which is the property this gate was reaching for. Logged as Ledger DX feedback — `12_LEDGER_DX_FEEDBACK.md` — since Ledger grades that explicitly and the failure mode (`FeeNotLoaded` instead of a clear "testnet sends unsupported") is a real, reproducible finding.

`HARD GATE 3.2 — The host holds no plaintext capability-minting key.` **Retargeted Sept 10** — `admincli/src/set-acl.ts`'s `BRAMBLE_GRANTER_PRIVATE_KEY` (the X25519 scalar `adr/0005`'s ACL digests derive from — more powerful than the Sepolia signing key, since it mints capability grants for any service any gateway trusts that granter for) is stored encrypted under `wallet-cli ring` on the admin's own machine, decrypted only in-memory at grant time. Not the node's WireGuard key — see the pivot note above for why.
**Test:** inspect the admin host's filesystem/`.env`; assert no plaintext `BRAMBLE_GRANTER_PRIVATE_KEY`. Document the LKRP rotation limitation (see `adr/0001-ledger-ring-vs-send-split.md`): after a ring member is removed, previously encrypted data cannot be decrypted, so key material must be re-encrypted on membership change.

**Code landed Sept 10, hardware round-trip still pending.** `admincli/src/ledger-ring.ts` wraps `wallet-cli ring encrypt`/`decrypt` (text over stdin/stdout, no plaintext intermediate file); `provision-granter-key.ts` is the one-time encrypt step; `set-acl.ts` now reads only `admincli/granter-key.ring` (gitignored ciphertext) and decrypts in-memory per call — `wireguardPrivateKeyFromEnv` and every plaintext-env code path for this key are deleted, not just unused. Filesystem check passes today: `git grep GRANTER_PRIVATE_KEY` outside docs turns up only usage strings, and no `.env` anywhere in the tree holds a value for it. **Not yet run against the real Nano S Plus** — the `ring encrypt`/`decrypt` round-trip for this specific key, and the "device unplugged, decrypt still works" claim, need the same live confirmation Gate 0.1's `.tmp/LEDGER_GATE0.1_REPORT.md` already gave the mechanism generically, just not yet for this exact file/key path.

`HARD GATE 3.3 — Describe "ring" accurately.` It is encryption, not signing. Signing goes through `wallet-cli send` with on-device confirmation.
**Test:** grep README and video script for any claim that `ring` signs anything.

**New, Sept 10 — the escalation loop, the other half of "human approves before permissions escalate":** an already-admitted agent's `CONNECT` outside its current ACL scope is denied and logged by `brambled/gateway`, which re-resolves ACL state fresh on every single call (no cache — confirmed by reading `gateway/acl.go` and `sidecar/client.go`). The admin runs the now-`ring`-protected `set-acl.ts` (Gate 3.2), and the agent's identical retry succeeds on the next attempt — live, no restart, same mechanism Gate 2.2 already proved for admission. Film this as one continuous shot: deny, admin runs `set-acl.ts`, retry succeeds. **Revised Sept 11 per Gate 3.1's block above:** the transaction itself signs via `SEPOLIA_PRIVATE_KEY`/viem, same as before — `wallet-cli send` can't sign a Sepolia transaction at all, so there is no physical-button-press moment in *this* step to film. The Ledger-specific, genuinely device-gated moment in this flow is the `ring decrypt` that recovers the granter's private key a few lines earlier in the same `set-acl.ts` run (Gate 3.2) — that's the shot worth filming, not the on-chain write after it. **Keep the deny→request surface advisory-only** (the gateway's existing stderr log line is enough — no queue, no daemon): it carries no authority, so it can be lossy or absent without changing the security model, which is the deliberate difference from the rejected decrypt-as-a-service dependency above. This overlaps Phase 5's Gate 5.1 (a real agent task through the mesh) rather than competing with it — budget accordingly.

**Discretion:** enrollment UX, whether the admin CLI wraps `wallet-cli` or instructs the user to run it.

## Phase 4 — Relay and x402 metering (Days 7–8 — Sept 10–11; **pulled forward, started Sept 9** — see Phase 3's deferral note above)

**Prerequisite:** Gate 0.4 (one real Blocky402 payment) — **✅ verified Sept 9, 2026**, see `10_DAY0_GATES.md`. This phase is now unblocked.

Split into sections, same convention as Phase 2 (ADR-backed sections A–D):

- **Section A (done)** — standalone Gate 0.4 proof, `scripts/gate0.4-blocky402-check/`. No relay integration.
- **Section B (done)** — the rendezvous relay's payment surface, `docs/adr/0007-rendezvous-relay-metering.md`. A new sidecar (`relay-sidecar/`, spawned by `relay -meter`) sells an x402-gated, self-contained HMAC token (0.01 testnet USDC via Blocky402) once per `Exchange()` call; `relay/token.go` verifies it entirely locally, no callback on the per-connection path. Satisfies Gate 4.1 and starts Gate 4.2.
- **Section C (done)** — data-plane relay metering, `docs/adr/0008-data-relay-metering.md`. One ephemeral UDP port per purchased session (`relay/datarelay.go`), priced per requested byte via `@x402/hono`'s `DynamicPrice` (`relay-sidecar/src/routes/datarelay.ts`, `pricing.ts`); `brambled` publishes the relay's address in place of its own for any `-relay-peer`-flagged peer (`brambled/main.go`'s `relayRoutedEndpoint`), and both sides arm a persistent WireGuard keepalive so the relay learns each side's address even though only one side initiates the real handshake. Gate 4.2 ✅ VERIFIED by a Sept 10 live run against the real relay binary and real Hedera testnet.
- **Section D — ✅ fully verified, including a real two-relay, two-machine, real-Hedera-settled kill-mid-transfer run (Sept 10, 2026).** `brambled` picks among a pool of data-relay candidates by price+latency (`brambled/datarelay.Pick`), querying each candidate's `relay-sidecar` `GET /price`; this half is ✅ VERIFIED live too. Kill-mid-transfer failover, mechanism closed Sept 10, 2026: `brambled/datarelay.WaitForStall` detects a dead relay via `wgnode.PeerInfo.RxBytes` staleness (not handshake staleness — a live session's key stays valid for minutes without a fresh handshake, so bytes-stopped-moving is the faster, more direct signal); the buying side (`watchDataRelayFailover`) re-points itself from the remaining pool, and the non-buying side (`watchDataRelayAdopt`) stays continuously present at the rendezvous relay to adopt whatever the buying side re-publishes — asymmetric on purpose, since a real relay hands out a fresh port per session and both sides independently buying was the first (broken) design. A live run against the real relay binary caught three real bugs this way (symmetric double-buy landing on two unconnected ports, the adopting side never arming a keepalive, and the exclusion set only ever excluding the currently-used relay instead of every relay tried) — all fixed. That same live run then hit a fourth, separate gap: rendezvous-token purchase was pinned to one relay-sidecar URL, so a node couldn't authenticate to a relay that outlived the one it was configured against. **That fourth gap is now fixed too** (same day) — root cause confirmed against source: each relay generates its own random `RENDEZVOUS_TOKEN_SECRET` (`relay/sidecar.go`), so a token bought from one relay's sidecar can never verify at another's; `exchangeAnyMetered` (replacing `rendezvous.ExchangeAny`) now buys a token from each relay's *own* sidecar before trying it. A first cached-token draft was caught in review before it shipped — tokens are single-use (`relay/token.go` rejects a replayed nonce), so caching would have failed on its second hit; the fix buys fresh every attempt instead. A second issue, only visible once a real *second* rendezvous relay existed in a test at all, made a two-relay regression flake roughly 1 run in 3: trying relays sequentially starved whichever came later in the list of its share of the retry window against the peer's own once-a-second offer — fixed by trying every relay concurrently instead, each getting the full timeout; 8/8 clean runs after. All covered by `TestDataRelayFailover_SwitchesToBackupRelayWhenPrimaryStalls`, which now runs two *independently metered* fake relays (each its own secret, single-use enforced) and kills the primary's rendezvous listener alongside its data relay, matching what killing a real relay process actually does.
  **Live run, Sept 10, 2026:** two real EC2 hosts (`ap-south-1`, arm64) — the existing `Bramble` host running the primary relay on a fresh port pair (`:9430`/`:7901`, so as not to disturb the already-running Gate 2.3 demo on that box) and a freshly-provisioned second host running the backup (`:9421`/`:7893`) — both `-meter -data-relay`, both paying the same funded Hedera testnet payee used in `relay-sidecar/README.md`'s Gate 4.1 run. `brambled demo-data-relay` ran locally against both real public IPs and real Gate 0.4 client credentials. The primary `relay` process was killed for real (confirmed dead via `ps`) mid-run; alice detected the stall, bought a fresh session from the backup, and reconnected — `=== Gate 4.3: PASS ===`. Both the pre-kill (primary) and post-kill/failover (backup) session purchases were independently confirmed as `SUCCESS` on the Hedera testnet mirror node, same client account debited, same payee account credited, two different relay-sidecars. This also reproduced the "Incidental finding" in `docs/adr/0008` — `pkill` on the primary's `relay` binary doesn't take its spawned `relay-sidecar` down with it (no SIGTERM handler forwarding), leaving an orphaned but harmless token-minting process; cleaned up manually, not fixed in `relay/main.go`. Full writeup and evidence: `docs/adr/0008`'s "Kill-mid-transfer failover" section.
- **Real relay discovery, Sept 10, 2026** — `-rendezvous`/`-data-relay` are no longer the only source of relay addresses. `brambled` now discovers both rendezvous and data relays from a relay registry via `sidecar/src/ens/relays.ts` (real event-log enumeration, not a manifest — see `docs/adr/0003`'s Consequence section, resolved), falling back to the CLI flags only as an explicit static override. `admincli/src/setup-relay-registry.ts`/`register-relay.ts` are the write side; EAC-gated (`grant-role.ts relay-registrar`) the same way device enrollment already is.
  **✅ Live run, Sept 10, 2026** — against the real dev tailnet (`bramble-dev-c91e55e9.eth`, registry `0xEdA4b78CcA4472522bf2b521A6D2987306579058`): deployed a real relay registry (`setup-relay-registry.ts`, relay registry `0x3276eA61D05001586d6eE450F982312B345746a6`, tx `0x633a76d5...aa7db`), registered `relay1.relays.bramble-dev-c91e55e9.eth` with both roles (`register-relay.ts --rendezvous 203.0.113.5:9420 --sidecar-url http://relay1.example.com:7891 --price-per-byte 1`, tx `0xe88443cc...abe23` + three `setText` txs), then called `resolveRelays()` with zero flags/manifest involved and got back exactly what was written on-chain: `{"rendezvous":[{"label":"relay1","address":"203.0.113.5:9420"}],"dataRelays":[{"label":"relay1","sidecarUrl":"http://relay1.example.com:7891","pricePerByte":"1"}]}` — confirmed both as a bare function call and through the real sidecar's `GET /relays` HTTP route.

`HARD GATE 4.1 — One real paid request settles on Hedera testnet via Blocky402, on camera.` Hedera qualification requirement. Not mocked, not local.
**Test:** E2E script prints a transaction ID viewable on HashScan.
**Note:** the rendezvous-relay fee (see Gate 4.2) already satisfies this on its own, every connection attempt — do not let this gate depend on the data-relay fallback firing naturally.
**✅ VERIFIED Sept 9, 2026** — Section B's live run (`relay-sidecar/README.md`): a real metered relay (`relay -meter`, spawning `relay-sidecar` itself), two real 0.01 USDC payments settled through Blocky402 (`0.0.7162784@1788941365.961612782`, `0.0.7162784@1788941368.403212861`, confirmed independently via the mirror node — real `CRYPTOTRANSFER`s of HTS token `0.0.429274`, not simulated), and a genuine bidirectional `brambled/rendezvous.Exchange` completing through the metered relay using the resulting real tokens — both sides learned the other's candidate exactly as an unmetered exchange would. HashScan: https://hashscan.io/testnet/transaction/0.0.7162784@1788941365.961612782. `go test ./relay/... ./brambled/...` green throughout, zero regressions to Gate 0.2/1.2/1.3/2.2/2.3 (metering is opt-in via `-meter`, default off).

`HARD GATE 4.2 — Metered, not flat.` Two transfers of different sizes cost different amounts.
**Test:** assert cost scales with bytes. Hedera awards extra points for metering over flat charges.
**Design constraint:** this must gate a session/bandwidth-allotment purchase, not individual packets — a literal per-packet HTTP 402 challenge is incompatible with real-time UDP tunneling. Settle periodically against accumulated usage (see `adr/0003-rendezvous-relay-split.md` and `11_SOURCE_NOTES.md` finding 5).
**Two fees, not one, per `adr/0003-rendezvous-relay-split.md`:** a small, roughly-flat rendezvous fee charged on every connection attempt (candidate exchange, regardless of whether hole punching succeeds), and a bytes-scaled data-relay fee charged only when hole punching fails and traffic actually needs forwarding. The rendezvous fee exists specifically so real, repeated, on-chain payment activity doesn't depend on the demo's topology happening to need a data relay — a laptop-to-VPS connection, for instance, will likely hole-punch (or just connect directly, since the VPS has no NAT) every time.
**Test, additional:** assert the rendezvous fee is charged on a connection that *successfully* hole-punches and never touches the data relay.
**✅ VERIFIED, Sept 10 2026** — Section C (`docs/adr/0008-data-relay-metering.md`). `go test ./relay/... ./brambled/...` and `pnpm verify` both green, plus a real live run: against the real `relay -meter -data-relay` binary and a real Hedera testnet payer, bought two sessions of very different sizes (1,000 and 100,000 bytes) from the same relay at the same per-byte price and got back two real, mirror-node-confirmable settlements of proportionally different amounts — cost scales with bytes, on-chain, not just in a unit test. `relay/datarelay_test.go`'s `TestDataRelay_LargerAllotmentForwardsMoreBytes` and `relay-sidecar/test/pricing.test.ts` cover the same claim at the unit level. `brambled/datarelay_endpoint_test.go`'s `TestRelayRoutedEndpoint_BothSidesReachEachOtherThroughDataRelay` is a real two-node netstack WireGuard handshake completing entirely through a hand-rolled test data relay — this caught and fixed a real bug pre-live-run (a first draft derived a relay-routed peer's own endpoint from the *peer's* published candidate instead of the relay address it just bought, which breaks whenever the peer has no direct address of its own — exactly the two-NAT topology this gate exists for) and a real design gap (WireGuard's passive/responder side never sends anything unprompted, so the relay would never learn its address without `wgnode.Node.SetPersistentKeepalive`, now armed on every relay-routed peer).

`HARD GATE 4.3 — Two relays, client chooses.` Even if both are yours, the selection mechanism must be real: different prices, client picks, connection survives killing one.
**Test:** kill the selected relay mid-transfer; assert failover.
**Demo requirement:** at least one demoed connection must use a topology that cannot hole-punch (e.g. two devices both behind NAT — see `10_DAY0_GATES.md` Gate 0.2's note on laptop-to-VPS being the easy case) so the data-relay path is shown live, not just asserted in a test.
**✅ VERIFIED, Sept 10 2026.** Selection and kill-mid-transfer failover both passed against two real relay processes on two machines with real Hedera-settled payments. The full mechanism, fixes, transaction evidence, and test coverage are recorded in Phase 4 Section D above and `docs/adr/0008-data-relay-metering.md`.

**Discretion:** pricing units, settlement batching, whether relays register in an ENS directory (recommended — Hedera extra points for agent discovery), HCS audit trail (extra points, optional).

## Phase 5 — Agent path (Days 8.5–9 — Sept 11–12)

`HARD GATE 5.1 — The agent is real but thin.` A genuine process needing private access, not an LLM demo. If agent framework code is being written on day 10, that is drift.
**Test:** the agent completes a real task through the mesh (query the private database, hit the internal API) and the transcript shows it.

**🔧 BUILT AND TESTED, live-chain recording remains.** `brambled/cmd/demo-agent` is a credential-free health-check worker that calls `cmd/demo-service` only through a Bramble localhost forward. `pnpm test:access-flow` exercises the matching real userspace-WireGuard lifecycle: allowed task, out-of-scope denial, live grant expansion, identical request success, and peer removal on revocation. Record the final two-machine run with the real ENS write before marking this gate fully verified.

`HARD GATE 5.2 — The agent holds no transferable credential.` No API key, no password, no long-lived token anywhere in the agent's environment.
**Test:** dump the agent's environment and config; assert the only secret is a node key that is worthless off-host and once revoked.

## Phase 6 — Demo, README, submission (Days 9–10 — Sept 12 to Sept 13 AM)

Video recorded by Sept 12; Sept 13 morning is upload/rehearsal buffer only. **Submissions close Sept 13, 16:00 UTC / 21:30 IST — hard.**

All gate tests green from a clean checkout. README covering setup, architecture, the on-chain boundary, measured revocation latency, and declared dependencies. `/docs/AI_USAGE.md` and `/docs/adr/`. Select ENS, Ledger, Hedera.

**Ledger-specific required deliverable, not yet tracked before this line: Developer Experience (DX) feedback.** Per `developers.ledger.com/ethonline`, Track 01 submissions require documentation of gaps, confusing flows, and specific improvement suggestions found while building on the Ledger Agent Stack — this is a submission requirement, not optional polish. `docs/12_LEDGER_DX_FEEDBACK.md` is the draft, seeded from the real Sept 10 hands-on run (`.tmp/LEDGER_GATE0.1_REPORT.md`): the Ledger Sync app prerequisite for `ring init` (undocumented, discovered empirically), and the gap between Key Ring's "headless by design, no human at the keyboard" marketing and its actual behavior on a genuinely fresh host (verified on a real second machine, not simulated). Finalize this alongside the README, not as an afterthought — it is graded, and it is also free: the content already exists from real testing, it just needs to be written up in the format Ledger asks for.

## Cut order

With 6.5 build days remaining as of Sept 7, treat this as a **decision to make by end of Sept 10** (end of Phase 3), not a day-11 contingency. If Phases 3 and 4 are both behind schedule on Sept 10, pre-commit to ENS + Hedera (the rendezvous fee alone satisfies Gate 4.1's "one real paid request" requirement) rather than discovering the cut on Sept 12 while the video is due.

1. HCS audit trail
2. Second relay (keep the selection mechanism, demo with one and say so)
3. Ledger Key Ring encryption → keep only `wallet-cli send` device confirmation
4. Ledger entirely → two slots
5. Agent path → mesh only, and drop Hedera's agent framing (weakens that slot badly)
6. x402 metering → ENS + Ledger only

**Never cut:** peer-side admission verification, the malicious-relay test, no-endpoints-on-chain, revocation, commit cadence.

## Future work (explicitly not scoped for this hackathon)

Ideas that are good next steps but not needed for any gate above, and would add integration risk if pulled in now. Recorded so they don't get silently forgotten or re-litigated in every conversation.

- **Auto-discover peers from the tailnet registry, instead of manual `-peer label=allowed-ip` flags.** Originally named two unverified blockers; only one remains. (1) **Resolved Sept 10, 2026, while building real relay discovery** — `PermissionedRegistry` has no enumeration view function, but it emits `LabelRegistered(tokenId, labelHash, label, owner, expiry, sender)` with the plaintext label on every registration, confirmed via a real `eth_getLogs` query against the deployed dev tailnet registry. Scanning that event works; see `sidecar/src/ens/relays.ts` for the pattern, directly reusable for devices. (2) **Still open and still the real blocker**: a mesh-IP assignment scheme, since `AllowedIP` today is operator-supplied and not derivable from ENS state (`admission/loop.go`'s `Peer` struct explicitly defers this to "a record schema question Phase 2 owns"). `admission.Loop.SyncOnce` already just iterates whatever's in `l.Peers`, so a discovery source slotting in later wouldn't require touching the sync/endpoint-resolution logic. Not a security downgrade to defer — the actual admission boundary is the WireGuard handshake against ENS-resolved keys either way; `-peer` only controls which labels a node bothers polling.
- **Automatic AllowedIP assignment**, e.g. a deterministic mesh-IP-from-ENS-record scheme, so operators stop hand-assigning `/32`s. Blocks on the same mesh-IP record schema as the point above.
- **Admin UI / web dashboard** — see all peers in a tailnet with live routing info (AllowedIPs, current endpoint, last handshake), node info (pubkey, ENS fullname, role/expiry), and rendezvous info (which relay(s) a peer is reachable through). Effectively a live view over what `brambled serve`'s stderr log already prints per-event, rendered instead of scrolled.
- **ENS registry explorer** scoped to a tailnet — browse registered devices/agents and their record state without needing `brambled resolve` per label, useful for debugging admission issues live during a demo or dev session.
- **"Test connection to peer" tooling** — a one-shot ping/handshake-status check against a specific peer from the UI or CLI, surfaced without needing to shell out to `ifconfig`/`ping`/`nc` manually (see `brambled/README.md`'s Gate 0.2 runbook, which currently requires exactly that).

## Reporting checkpoints

- Day 0 gates: 0.2/0.3 resolved Sept 5–6. **Gate 0.1 (Ledger device) and Gate 0.4 (Blocky402) are still open, due Sept 7–8** — with submissions closing Sept 13, an unverified Ledger rail or Blocky402 discovered late kills a sponsor slot, not just a phase.
- Gate 0.2 status: resolved Sept 6 (laptop-to-VPS; the harder two-NAT case remains open).
- End of Phase 1: ✅ malicious-relay test passing, plus measured revocation latency (3.7s @ 5s TTL, Sept 6).
- **Sept 8, 03:59 UTC / 09:29 IST: Project Check-in #1. Sept 11, 03:59 UTC / 09:29 IST: Check-in #2.** ETHGlobal showcase requirements — see `08_EVENT_RULES.md`.
- **End of Phase 3 (Sept 10): cut-order decision** (see Cut order).
- Immediately if EAC delegation does not actually restrict (Gate 2.1).
- Immediately if relay fallback rate is so high that direct connections rarely succeed — that changes the cost story in `01_WHY.md`.
